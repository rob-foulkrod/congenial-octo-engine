@{
    ViewData["Title"] = "About";
}
<section class="content-panel">
    <h1 class="mb-3">@ViewData["Title"]</h1>
    <p class="mb-2">
        Todo Flow is intentionally simple: a single ASP.NET Core MVC assembly backed by an in-memory provider.
    </p>
    <p class="mb-0">
        That keeps today's setup lightweight while leaving room for future providers such as Redis, Cosmos, and PostgreSQL.
    </p>
</section>
